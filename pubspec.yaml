name: md_pro
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.12.0
  go_router: ^17.2.0
  google_fonts: ^8.0.2
  animated_input_border: ^0.0.3
  flutter_quill: ^11.5.0
  flutter_md: ^0.0.8
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  device_preview: ^1.3.1
  shared_preferences: ^2.5.5
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
